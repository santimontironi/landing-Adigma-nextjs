generator client {
  provider = "prisma-client"
}

datasource db {
  provider = "postgresql"
}

model Message {
  id        Int      @id @default(autoincrement())
  email     String
  name      String
  surname   String
  phone     String
  content   String
  createdAt DateTime @default(now())
}
